export default defineNitroPlugin(async (nitroApp) => {
  console.log('defining MongoDB plugin')

  // nitroApp.hooks.hook('request', (event) => {
  //   console.log('MongoDB plugin request hook triggered')
  // })
})
